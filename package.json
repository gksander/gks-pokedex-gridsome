{
  "name": "gks-pokedex-gridsome",
  "private": true,
  "scripts": {
    "build": "gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "deploy": "netlify deploy --dir=dist --prod",
    "build-and-deploy": "yarn run svg-to-png && yarn run build && yarn run deploy",
    "svg-to-png": "node ./scripts/svg-to-png.js"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "gridsome": "^0.7.0",
    "lodash": "^4.17.15",
    "vuetify": "^2.1.11"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.11.2",
    "fs-extra": "^8.1.0",
    "klaw": "^3.0.0",
    "prettier": "^1.19.1",
    "sharp": "^0.23.3",
    "webpack-node-externals": "^1.7.2"
  }
}
